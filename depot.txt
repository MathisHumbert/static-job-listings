
// refaire ca
let roleArr = [];
for (let i of jobs) {
  if (!roleArr.includes(i.role)) {
    roleArr.push(i.role);
  }
}

let levelArr = [];
for (let i of jobs) {
  if (!roleArr.includes(i.role)) {
    roleArr.push(i.role);
  }
}
let languagesArr = [];
for (let i of jobs) {
  for (let x of i.languages) {
    if (!languagesArr.includes(x)) {
      languagesArr.push(x);
    }
  }
}
let toolsArr = [];
for (let i of jobs) {
  for (let x of i.tools) {
    if (!toolsArr.includes(x)) {
      toolsArr.push(x);
    }
  }
}

// faire réussi a cette fonction de iterate over un array comme ca dès que je modifie la valuer de l'array cela modifie dirctemnr ce dernier
// faire réussir a cliquer surement const les btn lors de leur création



let newJobs = null;
function displayFilterJobs() {
  console.log(this.dataset.name);
  let value = this.dataset.name;
  if (languagesArr.includes(value)) {
    console.log('test');
    newJobs = (newJobs || jobs).filter((job) => job.languages.includes(value));
    console.log(newJobs);
  }
  if (toolsArr.includes(value)) {
    console.log('test');
    newJobs = (newJobs || jobs).filter((job) => job.tools.includes(value));
    console.log(newJobs);
  }
  if (roleArr.includes(value)) {
    console.log('test');
    newJobs = (newJobs || jobs).filter((job) => job.role === value);
    console.log(newJobs);
  }
  if (levelArr.includes(value)) {
    console.log('test');
    newJobs = (newJobs || jobs).filter((job) => job.level === value);
    console.log(newJobs);
  }
  return displayJobListing(newJobs);
}


